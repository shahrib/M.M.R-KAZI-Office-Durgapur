# Local storage buckets

- `templates/` : uploaded DOCX templates and rendered DOCX files
- `generated-pdfs/` : generated PDF output
- `manual-pdf-uploads/` : manual PDF uploads from admin backend UI
- `image-assets/` : uploaded images used during generation

These folders are created automatically if missing.
